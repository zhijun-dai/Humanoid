# GPU 版运行依赖（Jetson Orin / 桌面通用）
#
# Jetson（JetPack 7.2，Ubuntu 24.04 + Python 3.12）：
#   python3 -m venv ~/robocup-venv && source ~/robocup-venv/bin/activate
#   pip install "numpy<=1.26.4"     # 板子预装 OpenCV 按 numpy 1.x 编译
#   pip install -r requirements-gpu.txt
#   python probe.py                 # 探测 OpenCV 是否带 CUDA
#
# 桌面 CPU 验证环境照用仓库根 requirements.txt，
# gpu_vision 自动走 CPU 回退，输出与 CPU 版一致。

opencv-python>=4.8,<5
numpy>=1.24
pyserial>=3.5
