# 机器人上机命令与参数

> 视觉侧跑在 Nano 板的 `jetson_orin_code` 仓库（`new_vision/`）里，通过 UDP 把
> `{vx, wz}` 发给走路策略。本文是**照着敲就能跑**的操作手册。
>
> 数据来源：链路与参数从代码实测（2026-09-24）；赛道几何由 1:1 渲染图与 DXF 圆弧互证。

---

## 0. 上车前置（每次先做）

```bash
cd ~/jetson_orin_code
git branch --show-current          # 确认分支，见 §7
source .venv/bin/activate

sudo ss -lunp | grep -E "5005|5006" || echo "端口干净"
sudo fuser /dev/video* 2>/dev/null  || echo "相机空闲"
```

**这台机器上相机是 `--camera 1`，没有 `/dev/video0`。** 插拔过就重新确认：

```bash
ls -l /dev/video*
for i in 1 2; do
  python - <<EOF
import cv2
c = cv2.VideoCapture($i, cv2.CAP_V4L2)
n = sum(1 for _ in range(5) if c.read()[0]) if c.isOpened() else 0
print("index $i opened=%s 读到 %d/5 帧" % (c.isOpened(), n))
c.release()
EOF
done
```

---

## 1. 三个终端

**A — 连接器**（视觉 → 策略的中转，50Hz 零阶保持）

```bash
python -u connector.py --vision-port 5006 --policy-port 5005
```

**B — 视觉**（相机 → 巡线检测 → PID → UDP）

```bash
python -u new_vision/jetson/run_policy_vision.py \
  --camera 1 --vx 0.4 --max-wz 0.5 --steer-full-scale-cm 9 --headless
```

**C — 走路策略**（收 UDP → ONNX → 串口 → 电机）

```bash
# 先干跑（不使能电机），确认方向
python -u humanoid_jetson_deploy/main.py --policy walking \
  --model humanoid_jetson_deploy/models/current_walking.onnx \
  --port /dev/ttyACM0 --command-source vision --udp-command-port 5005 \
  --no-plot --max-seconds 30

# 方向对了再上电机
python -u humanoid_jetson_deploy/main.py --policy walking \
  --model humanoid_jetson_deploy/models/current_walking.onnx \
  --port /dev/ttyACM0 --command-source vision --udp-command-port 5005 \
  --enable-motors --no-plot --max-seconds 10
```

⚠️ **`--model` 那一行要换成你实际在用的文件。** `current_walking.onnx` 是文档里的占位名
（README 说这个文件要自己从训练 checkpoint 导出），仓库里没有。`models/` 下实际只有：

```
policy.onnx                            policy_stable_walking_height_1.onnx
policy_0.4_feet_coliding.onnx          policy_front_feet_touching_ground.onnx
policy_height_3.onnx                   policy_shuffeling_0.2.onnx
policy_48.onnx   ← 48 输入，README 说这个接口不兼容
```

**SSH 下必须加 `--headless`（视觉）和 `--no-plot`（策略）**，否则 `cv2.imshow` 会 abort。

三个终端用 tmux：

```bash
tmux new -s robot      # Ctrl+b c 开新窗口，Ctrl+b 0/1/2 切换，Ctrl+b d 脱离
tmux attach -t robot   # 重连
```

**掉线不等于进程死**——tmux 里进程会继续跑，机器人继续走。**永远带 `--max-seconds`。**

### 只测转弯步态：一个终端就够

`--command-source fixed` 恒定发一组 `vx/wz`，**直接绕过视觉、连接器和 UDP**——
不需要 A 和 B，也没有 watchdog。用来单独看步态在转弯时垮不垮。

```bash
# 先干跑（不使能电机），确认方向和步态
python -u humanoid_jetson_deploy/main.py --policy walking \
  --model humanoid_jetson_deploy/models/<你跑巡线用的那个>.onnx \
  --port /dev/ttyACM0 --baud 921600 \
  --command-source fixed \
  --vx 0.4 --wz 0.5 --walk-seconds 8 \
  --no-plot --max-seconds 20

# 方向对了再上电机
python -u humanoid_jetson_deploy/main.py --policy walking \
  --model humanoid_jetson_deploy/models/<你跑巡线用的那个>.onnx \
  --port /dev/ttyACM0 --baud 921600 \
  --command-source fixed \
  --vx 0.4 --wz 0.5 --walk-seconds 8 --enable-motors \
  --no-plot --max-seconds 20
```

| 参数 | 说明 |
|---|---|
| `--command-source fixed` | 不接 UDP，恒定发 vx/wz |
| `--vx 0.4 --wz 0.5` | **最坏组合**：v/ω = **0.8 m 半径**，一圈 5.03 m / **12.6 秒** |
| `--wz` 符号 | `+0.5` 左转（逆时针），`-0.5` 右转（顺时针） |
| `--walk-seconds 8` | 8 秒后**自动置零**（急停保险），约 2/3 圈。跑整圈改 14 |
| `--max-seconds 20` | 到点进程退出并 `send_disable`（关电机） |

**先 kill 掉 A、B**，不然视觉白占 CPU：

```bash
pkill -f run_policy_vision.py; pkill -f connector.py
```

**读实际转速**（策略到底转没转到位）：

```bash
tail -f logs/motor_positions/motor_positions_*.csv | awk -F, '
NR>1 { y=$NF+0; t=$2+0
  if (pt>0 && t>pt) { d=y-py
    if(d>3.14159)d-=6.28319; if(d<-3.14159)d+=6.28319
    printf "%.2fs  yaw=%+.3f  |wz|=%.3f rad/s  (指令 0.500)\n", t, y, d/(t-pt) }
  pt=t; py=y }'
```

预期 `|wz| ≈ 0.5`。差得多 → 策略执行不了这个转速，视觉那边给满了也还是转不过来。

实测转弯半径 **R = 0.4 / |wz|**；0.5 对应 0.8 m，和赛道中线 0.776 m 只差 2.4 cm。

**扫一遍找拐点**：

```bash
for W in 0.2 0.35 0.5; do
  echo "=== wz=$W ==="
  python -u humanoid_jetson_deploy/main.py --policy walking \
    --model humanoid_jetson_deploy/models/<你跑巡线用的那个>.onnx \
    --port /dev/ttyACM0 --command-source fixed \
    --vx 0.4 --wz $W --walk-seconds 8 --enable-motors \
    --no-plot --max-seconds 20
  sleep 5
done
```

哪个 `wz` 开始出现趔趄/拖步，那个就是步态的拐点。

---

## 2. 参数表（视觉侧 `run_policy_vision.py`）

| 参数 | 默认 | 作用 |
|---|---|---|
| `--camera` | 0 | 相机索引。**这台机器用 1** |
| `--vx` | 0.4 | 有有效检测时的前进速度 m/s |
| `--max-wz` | 0.5 | 偏航角速度上限 rad/s，**硬上限 0.5** |
| `--steer-full-scale-cm` | 10 | **多少 cm 横向误差达到满舵**。越小转得越狠 |
| `--yaw-sign` | -1 | 图像→策略的转向符号。**反了就朝反方向拐** |
| `--step-len-cm` | 8 | 一步走多远，用于前瞻补偿。**必须等于策略的步距** |
| `--preview-gain` | 1 | 前瞻增益。转弯提前量太大就调小 |
| `--headless` | 关 | SSH 下必开 |
| `--max-seconds` | 0（不限） | 自停上限，**建议永远给** |
| `--connector-port` | 5006 | 连接器端口，改了 A 也要改 |

环境变量（覆盖上面同名的）：`CAM_IDX` `CAM_HEIGHT_CM` `CAM_PITCH_DEG` `CAM_VFOV_DEG`
`STEP_LEN_CM` `PREVIEW_GAIN` `JETSON_PID_STRAIGHT_KP/KI/KD` `JETSON_PID_CURVE_KP/KI/KD`
`JETSON_PID_I_CLAMP`

---

## 3. 推荐值（从赛道几何推出来的，不是试出来的）

### 赛道真实几何

轨道是**体育场形**（stadium）——两端各一个 180° 半圆，其余是直道，底部中间
收窄成限宽门。

| | 半径 | 怎么量的 |
|---|---|---|
| 外边界线两端 | **951 mm** | 对 1:1 渲染图做圆拟合，残差 0.1 px |
| 内边界线两端 | **601 mm** | 同上 |
| 差 | **350 mm** | = 道宽 ✓ 互证 |
| **中线** | **776 mm ≈ 0.78 m** | (951+601)/2 |

> 两个独立来源互证：DXF 里内 R=595 ↔ 外 R=945 的两段圆弧**圆心完全重合**，
> 中线 770mm，与渲染图量出的 776mm 一致。
>
> ⚠️ 底部的限宽门处有样条拼接的**折角**，曲率随测量窗口严格成正比
> （51→104→224→525→942），是纯折角特征，**不是能开的弯**，不要按它算。

其余尺寸：全长 6140mm，道宽 350mm，限宽门处两线最近 **228mm**（规则写 240mm）。

### 速度：`--vx` 就定 0.4

R = 0.776 m，0.4 m/s 需要 ω = 0.4 / 0.776 = **0.515 rad/s**，上限 0.5 —— **缺 3%**。
`connector.py:48` 和 `humanoid_jetson_deploy/command_source.py:21` 两处硬夹 ±0.5，
注释写明 `clamps commands to the ranges used during policy training`。

**这 3% 不值得动。** 缺口有多大代价：机器人按 v/ω = 0.8 m 的半径走（中线要 0.776），
两圆相切于入弯点，180° 处相距 **0.8 − 0.776 = 2.4 cm** —— 这就是整段弯里最大的外偏量。
车道半宽 17.5 cm，随便吃。

要真超 0.5，得改上面那两个**运动侧**文件；而且 0.5 是策略训练范围，超了就是喂分布外
输入，策略可能直接输出乱步态。不动。

### 真正的杠杆是 `--steer-full-scale-cm`（fs）

> ⚠️ **这一节 2026-09-24 修过一次，之前全错。**
> `fused_err` 是无量纲的（`near_err_px / 160`，实测 0.05~0.3），但
> `policy_bridge.py` 把它当 cm 用，于是实际增益比标称**小约 50 倍**：
> 弯道里 `wz` 只有 **0.03 rad/s**，而需要 0.45~0.5 —— 机器人物理上转不了弯，
> 只会一直往前走。已修（检测器新增 cm 版 `fused_err_cm`，`fs` 默认 50 → 10）。

`fs` 带单位，单位就是 `steer` 的单位（cm）；只有 `steer/fs` 这个比值无量纲。

```
wz = clip(steer / fs, -1, 1) × max_wz
steer = kp·err + ki·∫err + kd·d(err)/dt + preview_gain·step_len_cm·sin(ang)
        └───────────────── 四项全是 cm ─────────────────┘
⇒ 打满舵所需误差 e ≈ 1.06 × fs
```

| fs | 打满舵所需横向误差 | 评价 |
|---|---|---|
| 15 | 16 cm | 接近车道半宽 17.5cm，转弯迟钝 |
| **9** | **9.6 cm** | **推荐起步值** |
| 6 | 6.4 cm | 更狠，直道可能开始摆 |

### 推荐起步值

```bash
--vx 0.4  --max-wz 0.5  --steer-full-scale-cm 9
```

**注意 `steer` 是按"横向误差"的尺度算的，不是横向误差本身** —— 弯道时航向项和
前瞻项都会贡献。所以过弯看日志里的 `wz` 够不够 0.45，不够就继续降 fs。

嫌进弯晚半拍再动 `--preview-gain`：前瞻项 `preview_gain × step_len_cm × sin(ang)`
是纯前馈的提前量，调大让机器人**更早**开始转。代价是调太大时直道末端会提前拐。

---

## 4. 调参流程

看**终端 B** 的日志：

```
[vision -> connector] vx=+0.400 m/s wz=+0.xxx rad/s steer=+xx.xxcm conf=0.xxx lost=0
```

| 现象 | 原因 | 改什么 |
|---|---|---|
| 过弯 `wz` 到不了 ±0.4 | fs 还没降够 | `--steer-full-scale-cm` 继续往下（9 → 7 → 6） |
| 直道 `wz` 一直在 ±0.5 来回 | 太热 | `--steer-full-scale-cm` 往上调（9 → 12 → 15） |
| 直道左右摆 | 增益太热 | 提 fs |
| 两端半圆往外甩 | 弯中 3% 饱和，最多外偏 2.4cm | 正常现象，不用管；偏得多说明 fs 没降够 |
| 进弯总是晚半拍 | 前瞻不够 | `--preview-gain` 往上调（1 → 1.5 → 2） |
| 转弯提前量太大/甩过头 | 前瞻过冲 | `--preview-gain` 调小（1 → 0.5） |
| 机器人朝反方向拐 | 符号反了 | `--yaw-sign` 从 -1 改成 1，重启 B |
| `lost` 经常 > 0 | 丢线 | 见 §6 的第 3 条 |

**每次只改一个参数**，否则不知道是哪条起作用。

---

## 5. 日志怎么读

**终端 B**（每 0.5 秒一行）

```
vx     发给策略的前进速度
wz     发给策略的偏航角速度      ← 过弯看这个够不够
steer  PID 输出（cm）。**到 ±fs 就已经满舵**（fs 默认 10），不用等到 ±50
conf   检测置信度，<0.15 基本算丢线
lost   连续丢线帧数，>0 时 vx/wz 会被置零
```

**终端 C**（策略侧）

```
policy_target_velocity=[vx=..., wz=...]   策略实际收到的指令
infer=XX.XXXms                            ONNX 推理耗时（不是循环耗时）
WARNING: policy deadline missed by X ms   50Hz 没跟上
crc_errors=0                              串口正常
```

**CSV 遥测**（策略侧唯一能实时看的通道）

```bash
tail -f logs/motor_positions/motor_positions_*.csv | awk -F, '{print $2" s  step "$3"  yaw "$NF}'
```

列：`elapsed_s, step, state_sequence, target_<12关节>_rad, actual_<12关节>_rad,
imu_accel_xyz, imu_roll/pitch/yaw`

- **实际频率** → `step / elapsed_s`
- **转没转** → `imu_yaw_rad` 差分（会漂，判断"在不在转"够用）
- ⚠️ CSV 里**没有速度列、没有步长列**；实际前进速度和真实落脚点，整个系统都没测

---

## 6. 常见故障

**1. `can't open camera by index`**
这台机器没有 `/dev/video0`，用 `--camera 1`。详见 §0 的探测脚本。

**2. `OSError: [Errno 98] Address already in use`**
上次的 connector 没退干净：

```bash
sudo ss -lunp | grep 5006      # 看是谁
pkill -f connector.py
```

**3. `qt.qpa.xcb: could not connect to display` + abort**
SSH 下没加 `--headless`。

**4. 机器人不动 / 走走停停**
视觉频率掉了。看日志的 `fps=`：**低于 4 Hz 会被下游 watchdog 判死发零速**。
图卡检测器单帧 ~90ms，是巡线的 3~4 倍，所以它隔 3 帧才跑一次。如果 `fps` 很低，
先确认没有别的程序抢相机或 CPU。

**5. 转弯冲出去**
按 §3/§4 调。先看弯道里日志的 `wz`：
- **`wz` 上不去 0.4** → 降 `--steer-full-scale-cm`（9 → 7 → 6）。另一种可能是**没 pull
  新代码** —— 修单位前 `wz` 只有 0.03 rad/s，怎么调参数都没用。
- **`wz` 到 ±0.5 了还冲出去** → 不是增益问题，是策略执行不了这个转速，只能降 `--vx`
  （但这会让竞速分变少，先确认 `--vx 0.4` 时实际转弯半径是不是 0.8m）。

`--vx` 保持 0.4：弯中 3% 饱和只让机器人外偏 2.4cm，不是冲出去的原因。

**6. 想全部关掉**

```bash
# 顺序：先关策略（它占串口、退出时会关电机），再关视觉，最后连接器
pkill -f "humanoid_jetson_deploy/main.py"; sleep 1
pkill -f "run_policy_vision.py"
pkill -f "connector.py"
```

⚠️ **不要用 `kill -9`**——策略收不到 SIGTERM 就不会发 `send_disable`，**电机会保持使能**。

---

## 7. 分支说明（机器人仓库 `Jetson_orin_nano_code`）

| 分支 | 内容 |
|---|---|
| `main` | 原始状态 |
| **`vision`** | 最新视觉代码 + 相机参数 + PID 修复 + **单位修复**。**跑巡线用这个** |
| `vision-test-card-stop` | 在 `vision` 基础上加"看到图卡就锁死停车"（一次性测试用）。**已包含上面全部修复** |
| 本地 `Robocup` 的 `main` | 开发仓，视觉源码在这儿，改完再往机器人仓库同步 |

```bash
git checkout vision && git pull        # 巡线
git checkout vision-test-card-stop && git pull   # 图卡停车测试
```

**上车前逐条确认**（这两条不对，参数怎么调都是白调）：

```bash
git log --oneline -1        # 应能看到 "Fix fused_err unit mismatch…"
python -c "import sys; sys.path.insert(0,'new_vision/jetson'); \
from line_detector_v1_warp import LineDetector; \
d=LineDetector(1280,720); print('err_scale_cm =', round(d.err_scale_cm,2))"
# 期望 ~52.8；报 AttributeError 说明代码是旧的
```

---

## 8. 已知未解 / 待确认

- **`v_cmd_mmps` 接收端认不认**：协议里标注 "reserved"，`run_robot.py` 一直发 0。
  "远处见到图卡减速"这条策略因此**默认不生效**（`CARD_SLOW_MMPS=0`）。
- **图卡动作执行不了**：`humanoid_jetson_deploy/command_source.py` 明确丢弃 `qr` 字段，
  所以识别到图卡只能停车，做不了举左手/抬腿等动作。要真做动作得改运动侧。
- **转弯没有速率限制**：小车那套有 `RATE_LIM`，机器人这条链路没有。`steer_full_scale_cm`
  调小后如果直道开始摆，这是下一个要补的。
- **实际速度和真实步长拿不到**：整车没有任何测速/测步长的环节。
- **图卡只有一张三角形卡的真实数据**，其余 5 种形状的分类未经验证。
